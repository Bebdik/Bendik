// astro.config.mjs
import { defineConfig } from 'astro/config';

export default defineConfig({
  // If you don't need any custom markdown plugins,
  // you can omit the `markdown` section entirely.
});
